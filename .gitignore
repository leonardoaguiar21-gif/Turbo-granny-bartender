node_modules/
.DS_Store

# Backend database (local only — each dev has their own)
backend/db.json

# Environment secrets
.env
backend/.env

# Build output
frontend/dist/

# Bun lockfile (optional to commit — remove this line if you want reproducible installs)
# bun.lockb
